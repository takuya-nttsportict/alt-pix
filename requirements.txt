# ── Stream ingestion ──────────────────────────────────────────────────────────
av>=12.0.0

# ── Detection (YOLOX, Apache 2.0) ─────────────────────────────────────────────
# torch/torchvision are pre-installed in the base image (nvcr.io/nvidia/pytorch)
# Listed here for documentation; pip will skip if already satisfied.
torch>=2.3.0
torchvision>=0.18.0

# ONNX runtime for YOLOX inference (GPU build)
onnxruntime-gpu>=1.18.0

# ── Tracking (boxmot bundles StrongSORT, ByteTrack, OSNet ReID) ───────────────
boxmot>=10.0.0

# ── OCR ───────────────────────────────────────────────────────────────────────
easyocr>=1.7.0

# ── Video / image processing ──────────────────────────────────────────────────
# Use headless build to avoid pulling in GUI libs
opencv-python-headless>=4.10.0

# numpy 2.x required by boxmot and opencv 4.10+
numpy>=2.0.0

scipy>=1.13.0

# ── Utilities ─────────────────────────────────────────────────────────────────
tqdm>=4.66.0
pyyaml>=6.0.2
loguru>=0.7.0

# ── Dataset download (optional, only needed for fine-tuning) ──────────────────
# roboflow>=1.1.0
